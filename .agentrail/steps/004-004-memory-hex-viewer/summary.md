Added hex dump memory viewer panel with region selection dropdown (code, eval stack, call stack, globals, heap), 16 bytes per row with address column and ASCII sidebar, PC/pointer highlighting. Added step-over and step-out p-code stepping modes using call depth tracking. All regions render correctly, dropdown switches between them, highlighting works for PC in code region and stack pointers in their respective regions.