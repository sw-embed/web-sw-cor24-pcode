Add a demo registry with embedded .spc/.pasm test programs. Create a dropdown in the control bar to select from available demos. Each demo should include a name, description, and embedded source. Include at least the pvm.s VM and add a simple test program (e.g., factorial, fibonacci). Wire the demo selector to reset the emulator and load the selected program. Also add a UART input field so users can type input that gets fed to the VM via the emulator's UART.