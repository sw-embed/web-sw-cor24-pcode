Add call frame panel and memory region map to the p-code VM debugger. Specifically: (1) Add a Call Frame panel showing the current call stack by walking call_stack memory from call_stack_base to CSP. Each call frame entry should show: return address, frame pointer, procedure context. Display as a collapsible list with the current frame expanded. (2) Add a Memory Region Map panel showing a segmented bar visualization of VM memory layout: code segment, eval stack, call stack, globals, heap. Show address ranges and current usage for each region. Calculate regions from vm_state pointers (code_base, eval_stack_base, call_stack_base, GP, HP). (3) Add instruction count display to the control bar showing total COR24 instructions executed. (4) Improve eval stack display with change highlighting (highlight values that changed since last step). Pattern reference: ~/github/sw-vibe-coding/web-tf24a for panel patterns and memory visualization.